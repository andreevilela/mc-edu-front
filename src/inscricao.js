import * as React from 'react';
import { Button, View, Text } from 'react-native';

export default props => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>Formulário de inscrição de turma</Text>
        </View>
    );
}